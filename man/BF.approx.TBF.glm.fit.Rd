% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aux_fun_BF.R
\name{BF.approx.TBF.glm.fit}
\alias{BF.approx.TBF.glm.fit}
\title{Logarithm of the test-based Bayes factor (TBF) in glm}
\usage{
BF.approx.TBF.glm.fit(
  y,
  X,
  family = binomial(link = "logit"),
  n = length(y),
  k = ncol(X) - 1,
  lTBF,
  weights = rep(1, length(y)),
  offset = rep(0, length(y)),
  control = glm.control()
)
}
\arguments{
\item{y}{Response variable in the linear model.}

\item{X}{Full imputed covariance matrix for a particular model including
the fixed terms and the intercept.}

\item{family}{String, function or the call to a family function among
\code{\link[stats]{family}} to specify the error distribution and link
function to be used in the model.}

\item{n}{Number of observations.}

\item{k}{Number of model-specific coefficients.}

\item{lTBF}{Function to compute log-TBF, with the corresponding fixed parameters.}

\item{weights}{NULL or numeric vector of the same length as \code{y} to
specify the weights to be used in the glm fitting process.}

\item{offset}{NULL or a numeric vector of the same length as \code{y} to
specify an a priori known component included in the glm fitting process.}

\item{control}{List of parameters for controlling the glm fitting process.
It is set to \code{[stats]{glm.control()}} by default.}
}
\value{
\code{BF.approx.TBF.glm.fit} returns, in logarithmic scale, the TBF
approximation of the Bayes factor in generalized linear models for a given
model through \code{X}.
}
\description{
Computes the logarithm of the TBF approximation of Bayes factors for
complete data in generalized linear models.
}
\examples{
#Indian Prime Diabetes Data from VIM's package

Xdiab = VIM::diabetes[,c("Pregnancies", "Glucose", "Insulin")]
f <- Outcome ~ Pregnancies + Glucose + Insulin
imp1 <- mice.imputation(X = Xdiab, formula = f, n.imp = 1)

glmnull <- glm(Outcome ~ 1, data = VIM::diabetes, family = binomial(), y = T)
lTBF <- function (k, dev) lTBF.gfixed(g = length(glmnull$y), k, dev, devnull = 0)
lBF.imp1 <- BF.approx.TBF.glm.fit(y = glmnull$y, X = imp1$imputation.array[,,1],
  family = binomial(), lTBF = lTBF)

}
\references{
Held, L., Sabanés Bové, D. and Gravestock, I.
(2015)<DOI:10.1214/14-STS510> Approximate Bayesian Model Selection with the
Deviance Statistic. Statistical Science, 30(2): 242–257.
}
\seealso{
Use \code{\link[MissingBVS]{lBF.approx}} to compute the average Bayes
factor for missing data. Use \code{\link[MissingBVS]{MissingBvs.glm}} for
an exact computation of the model  posterior distribution in the VS problem
(recommended when p<20).
}
\author{
Carolina Mulet
Maintainer: <Carolina.Mulet1@alu.uclm.es>
}
