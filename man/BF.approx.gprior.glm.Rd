% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aux_fun_BF.R
\name{BF.approx.gprior.glm}
\alias{BF.approx.gprior.glm}
\title{Logarithm of the g-prior Bayes factor in glm}
\usage{
BF.approx.gprior.glm(
  y,
  X,
  family = binomial(link = "logit"),
  logmargnull,
  prior.betas = BAS::robust(n = length(y)),
  k = ncol(X) - p0,
  p0 = 1L,
  weights = rep(1, length(y)),
  offset = rep(0, length(y)),
  control = glm.control(),
  laplace = 0L
)
}
\arguments{
\item{y}{Response variable in the linear model.}

\item{X}{Full imputed covariance matrix for a particular model including
the fixed terms and the intercept.}

\item{family}{String, function or the call to a family function among
\code{\link[stats]{family}} to specify the error distribution and link
function to be used in the model. For now, only available the implemented
families in \pkg{BAS}: \code{binomial(link = "logit")},
\code{poisson(link = "log")} and \code{Gamma(link = "log")}.}

\item{logmargnull}{Log-marginal likelihood of the null model considered.}

\item{prior.betas}{Prior distribution for model-specific coefficients.
Options include \code{\link[BAS]{g.prior}}, \code{\link[BAS]{CCH}},
\code{\link[BAS]{robust}} and \code{\link[BAS]{intrinsic}} among others.
See \code{\link[BAS]{BAS}} for more details.}

\item{k}{Number of model-specific coefficients.}

\item{p0}{Number of fixed covariates (including the intercept).}

\item{weights}{NULL or numeric vector of the same length as \code{y} to
specify the weights to be used in the glm fitting process.}

\item{offset}{NULL or a numeric vector of the same length as \code{y} to
specify an a priori known component included in the glm fitting process.}

\item{control}{List of parameters for controlling the glm fitting process.
It is set to \code{[stats]{glm.control()}} by default.}

\item{laplace}{Logical variable to access the Laplace approximation to the
marginal likelihood of \pkg{BAS}. See \code{\link[BAS]{bas.glm}}
for more details.}
}
\value{
\code{BF.approx.gprior.glm} returns, in logarithmic scale, the exact
value of the Bayes factor derived from assigning a chosen g-prior by
\code{prior.betas} in generalized linear models for a given model through
\code{X}.
}
\description{
Computes the logarithm of the Bayes factors derived from a given g-prior
for complete data in generalized linear models.
}
\examples{
#To be completed

}
\references{
Clyde, M (2025)<DOI:10.32614/CRAN.package.BAS> BAS: Bayesian
Variable Selection and Model Averaging using Bayesian Adaptive Sampling.
R package (CRAN).

Li, Y. and Clyde, M. (2018)<DOI:10.1080/01621459.2018.1469992> Mixtures of
g-priors in Generalized Linear Models. Journal of the American Statistical
Association. 113:1828-1845
}
\seealso{
Use \code{\link[MissingBVS]{lBF.approx}} to compute the average Bayes
factor for missing data. Use \code{\link[MissingBVS]{MissingBvs.glm}} for
an exact computation of the model  posterior distribution in the VS problem
(recommended when p<20).
}
\author{
Carolina Mulet
Maintainer: <Carolina.Mulet1@alu.uclm.es>
}
