indf <- apply(positionsfac, MARGIN = 1, FUN = function(x) head(which(x == 1), n = 1))
#representant saturated models for each factor (row)
satmodels.repr <- positionsfac
for (j in 1:L) {
satmodels.repr[j, indf[j]] <- 0
}
satmodels.repr
for (j in 1:L) satmodels.repr[j, indf[j]] <- 0
x <- 6
2^4
x <- 16
p <- 4
#x is the number to get its binary expression, p is the number of variables,
#namesxnotnull is the name of the p competing vars and NAvars, the ones with NAs.
#If namesxnotnull = NULL and NAvars = NULL, the row "bin" equals integer.base.b_C
if (x == 0) {
res <- numeric(p)
} else {
ndigits <- (floor(logb(x, base = 2)) + 1)
res <- numeric(ndigits)
for (i in 1:ndigits) {
res[i] <- (x %% 2)
x <- (x %/% 2)
}
res <- c(res, numeric(p - ndigits)) #variables active
}
x <- 15
#x is the number to get its binary expression, p is the number of variables,
#namesxnotnull is the name of the p competing vars and NAvars, the ones with NAs.
#If namesxnotnull = NULL and NAvars = NULL, the row "bin" equals integer.base.b_C
if (x == 0) {
res <- numeric(p)
} else {
ndigits <- (floor(logb(x, base = 2)) + 1)
res <- numeric(ndigits)
for (i in 1:ndigits) {
res[i] <- (x %% 2)
x <- (x %/% 2)
}
res <- c(res, numeric(p - ndigits)) #variables active
}
res
resNA <- res * (namesxnotnull %in% NAvars) #variables in model with NA
positionsfac %*% res
positionsfac
positionsfac * res
resfact <- positionsfac * res
resfact
cbind(matrix(c(res, resNA), byrow = T, nrow = 2, ncol = p,
dimnames = list(c("bin","NA"), namesxnotnull)), resfact)
resNA <- res
cbind(matrix(c(res, resNA), byrow = T, nrow = 2, ncol = p,
dimnames = list(c("bin","NA"), namesxnotnull)), resfact)
rbind(matrix(c(res, resNA), byrow = T, nrow = 2, ncol = p,
dimnames = list(c("bin","NA"), namesxnotnull)), resfact)
rbind(matrix(c(res, resNA), byrow = T, nrow = 2, ncol = p), resfact)
comb.res <- rbind(matrix(c(res, resNA), byrow = T, nrow = 2, ncol = p), resfact)
dimnames(comb.res) <- dimnames = list(c("bin","NA",1:nrow(positionsfac)), namesxnotnull)
dimnames(comb.res) <- list(c("bin","NA",1:nrow(positionsfac)), namesxnotnull)
cres <- rbind(matrix(c(res, resNA), byrow = T, nrow = 2, ncol = p), resfact)
dimnames(cres) <- list(c("bin","NA",1:nrow(positionsfac)), namesxnotnull)
cres
NULL * res
resfact <- NULL * res
cres <- rbind(matrix(c(res, resNA), byrow = T, nrow = 2, ncol = p), resfact)
cres
nrow(NULL)
dimnames(cres) <- list(c("bin","NA",1:nrow(NULL)), namesxnotnull)
dimnames(cres) <- list(c("bin","NA",1:nrow(resfact)), namesxnotnull)
dimnames(cres) <- list(c("bin","NA",1:dim(resfact)[1]), namesxnotnull)
positions
positionsfac
rownames(positionsfac)
positions %*% model
aux <- positions %*% model
aux > 0
aux[rownames(positionsfac)]
aux[rownames(positionsfac),]
l
fact.names <- names(l)
aux[fact.names,]
positionsx
positions[positionsx,]
d <- positionsfac %*% model #levels active of factors
d
positions
positionscov <- positions[positionsx,,drop=F]
positionscov
positionscov %*% model
c(positionscov %*% model, d)
d
as.vector(positionsfac %*% model)
d <- as.vector(positionsfac %*% model) #levels active of factors
gt <- c(positionscov %*% model, d) > 0 #covariates and/or factors active
t <- d > 0 #active factors
t
any(satmodels.repr[j,] != positionsfac[j,] * model)
dataS97$EcOrg <- as.factor(dataS97$EcOrg)
formula
devtools::load_all()
formula <- as.formula(formula)
null.model <- as.formula(null.model)
#Response in the null model and full model must coincide
if (formula[[2]] != null.model[[2]]){
stop("The response in the full and null model does not coincide.\n")
}
env <- environment()
#Build matrices and objects needed later on
buildmatrices.list <- buildmatrices(formula, null.model, data, marginal.factors)
list2env(buildmatrices.list, envir = env)
#Check arguments and compute n.keep if needed
n.keep <- checkBvsarguments(p, p0, namesnull, namesx, n.keep, q)
#Check model priors chosen and define the functions to be used
lprior.models <- checkforprior.models(prior.models, priorprobs, q)
mF <- L > 0 & marginal.factors
#Check if factors present and if marginalization of their probabilities. Define model prior
lp.model <- checkmarg.factorsprior(mF, prior.models.dummies, l, positions[positionsx,,drop = F],
positionsfac, satmodels.repr, lprior.models)
#Evaluate the null model:
lmnull <- lm(formula = null.model, data, y = TRUE, x = TRUE)
#The response variable
y <- lmnull$y; obsnotNA <- names(y) #without missings
n <- length(y)
SS0 <- crossprod(lmnull$residuals) #SSE of the null model
#Check approx method and priors chosen and define the function to be used
lBF <- checkforprior.betas.lm(BF.method, prior.betas, n, p, p0, y, SS0)
X.full <- X.full[obsnotNA,] #remove NA obs from null model
#check for missings and define competing variables with NAs
NAvars <- checkformissings(y = framenull[,1], framenull[,-1], X.full)
#Define function to get binary expression for each model
num2bin.model.fun <- function (x) num2bin.model(x, p = p,
namesxnotnull = namesxnotnull,
NAvars = NAvars)
#Imputation step
if (anyNAvar <- !is.null(NAvars)) {
if (is.null(imp.datasets)) { #if there are no given imputations, build them
imputation.list <- buildimputation(NAvars, formula, data, imp.predict.mat, n.imp,
maxit, n, q, p0, imp.mice.method, imp.seed,
parallelmice, n.core, obsnotNA, ordvars)
} else imputation.list <- extimputation(formula, imp.datasets, n0 = dim(data)[1],
framefull, ordvars, obsnotNA, p0, NAvars)
list2env(imputation.list, envir = env)
}
if (n.imp > 1) {
#function to compute log(BFa0) for a given model as an average of BF computed
#by lBF over the imputed datasets
lBF.method <- function (model) lBF.av(model,
imputation.array = imputation.array,
lBF = lBF, p0 = p0, n.imp = n.imp)
} else lBF.method <- function (model) lBF(k = length(model),
X = imputation.array[,c(1:p0, model+p0),])
#Info:
cat("Info. . .\n")
if (mF) {
cat("Most complex model has a total of", q + q0, "covariates and/or factors.\n")
} else cat("Most complex model has a total of", q + q0, "competing variables.\n")
if (q0 == 1) {
cat("From those 1 is fixed (the intercept) and we should select from the remaining",
q, ".\n")
} else cat("From those", q0, "are fixed and we should select from the remaining",
q, ".\n")
if (mF) {
cat("  Numerical covariates:", depvars[positionsx], "\n")
cat(" Factors:", depvars[!positionsx], "\n")
} else  cat("  Competing variables:", depvars, "\n")
cat("The problem has a total of", 2^q, "competing models.\n")
cat("Of these, the ", n.keep, "most probable (a posteriori) are kept.\n")
#Compute exact posterior distribution and normalizing constant
posterior.list <- exact.posterior.comput(p, num2bin.model.fun, lBF.method,
lp.model, X0, X.full, lBF)
list2env(posterior.list, envir = env)
all.models.PM
positionsf <- colSums(positions %*% t(positions)) == 0 #TRUE if factor
positionsf
positions %*% t(positions)
positionsf <- colSums(positions %*% t(positions)) > 1 #TRUE if factor
positionsf
q
i<-1
modelsprob <- buildmodelsmatrix(num2bin.model.fun, q)
modelsprob
q
modelsprob <- buildmodelsmatrix(num2bin.model.fun, q)
num2bin.model.fun
cbind(t(sapply(seq_len(2^q)-1,
function(j) num2bin.model(j, q, NULL, NULL)["bin",])), numeric(2^q))
modelsprob <- buildmodelsmatrix(q)
q
devtools::load_all()
modelsprob <- buildmodelsmatrix(q)
modelsprob
positionsf <- colSums(positions %*% t(positions)) > 1 #TRUE if factor
positionsf
modeli <- modelsprob[i, seq_len(q)]
modeli
modeli * positionsf
cf.models.PM
#compute models matrix at the covariate-factor level
cf.models.PM <- all.models.PM[,seq_len(p)] %*% t(positions)
cf.models.PM <- cbind(cf.models.PM, all.models.PM[,p+1])
colnames(cf.models.PM)[q+1] <- "Post"
cf.models.PM
any(modeli * positionsf)
modeli * positionsf
positionsf
any(modeli * positionsf > 0)
positionsf
modeli
modeli * positionsf
modeli * positionsf > 0
modelsprob == modeli
positionsf <- colSums(positions %*% t(positions)) > 1 #TRUE if factor
#search models on modelsprob
for (i in seq_len(2^q)) {
modeli <- modelsprob[i, seq_len(q)]
if (any(modeli * positionsf > 0)) { #if factor
j <- which(apply(cf.models.PM[, seq_len(p)], 1, function(x) all(x == modeli)))
modelsprob[i, q + 1] <- sum(cf.models.PM[j, q + 1])
} else  modelsprob[j, q + 1] <- cf.models.PM[i, q + 1]
}
warnings()
positionsf <- colSums(positions %*% t(positions)) > 1 #TRUE if factor
modelsprob <- buildmodelsmatrix(q)
positionsf <- colSums(positions %*% t(positions)) > 1 #TRUE if factor
#search models on modelsprob
for (i in seq_len(2^q)) {
modeli <- modelsprob[i, seq_len(q)]
if (any(modeli * positionsf > 0)) { #if factor
j <- which(apply(cf.models.PM[, seq_len(q)], 1, function(x) all(x == modeli)))
modelsprob[i, q + 1] <- sum(cf.models.PM[j, q + 1])
} else  modelsprob[j, q + 1] <- cf.models.PM[i, q + 1]
}
modelsprob
modelsprob <- buildmodelsmatrix(q)
positionsf <- colSums(positions %*% t(positions)) > 1 #TRUE if factor
#search models on modelsprob
for (i in seq_len(2^q)) {
modeli <- modelsprob[i, seq_len(q)]
if (any(modeli * positionsf > 0)) { #if factor
j <- which(apply(cf.models.PM[, seq_len(q)], 1, function(x) all(x == modeli)))
modelsprob[i, q + 1] <- sum(cf.models.PM[j, q + 1])
} else  modelsprob[i, q + 1] <- cf.models.PM[i, q + 1]
}
modelsprob
cf.models.PM
all.models.PM
#compute models matrix at the covariate-factor level
cf.models.PM <- all.models.PM[,seq_len(p)] %*% t(positions)
cf.models.PM <- cbind(cf.models.PM, all.models.PM[,p+1])
colnames(cf.models.PM)[q+1] <- "Post"
cf.models.PM
seq_len(2^q)-1
seq_len(2^q)-1[-1]
cbind(t(sapply(seq_len(2^q),
function(j) num2bin.model(j, q, NULL, NULL)["bin",])), numeric(2^q))
cbind(t(sapply(seq_len(2^q-1),
function(j) num2bin.model(j, q, NULL, NULL)["bin",])), numeric(2^q))
cbind(t(sapply(seq_len(2^q-1),
function(j) num2bin.model(j, q, NULL, NULL)["bin",])), numeric(2^q-1))
cbind(t(sapply(c(seq_len(2^q-1),0),
function(j) num2bin.model(j, q, NULL, NULL)["bin",])), numeric(2^q))
devtools::load_all()
formula <- as.formula(formula)
null.model <- as.formula(null.model)
#Response in the null model and full model must coincide
if (formula[[2]] != null.model[[2]]){
stop("The response in the full and null model does not coincide.\n")
}
env <- environment()
#Build matrices and objects needed later on
buildmatrices.list <- buildmatrices(formula, null.model, data, marginal.factors)
list2env(buildmatrices.list, envir = env)
#Check arguments and compute n.keep if needed
n.keep <- checkBvsarguments(p, p0, namesnull, namesx, n.keep, q)
#Check model priors chosen and define the functions to be used
lprior.models <- checkforprior.models(prior.models, priorprobs, q)
mF <- L > 0 & marginal.factors
#Check if factors present and if marginalization of their probabilities. Define model prior
lp.model <- checkmarg.factorsprior(mF, prior.models.dummies, l, positions[positionsx,,drop = F],
positionsfac, satmodels.repr, lprior.models)
#Evaluate the null model:
lmnull <- lm(formula = null.model, data, y = TRUE, x = TRUE)
#The response variable
y <- lmnull$y; obsnotNA <- names(y) #without missings
n <- length(y)
SS0 <- crossprod(lmnull$residuals) #SSE of the null model
#Check approx method and priors chosen and define the function to be used
lBF <- checkforprior.betas.lm(BF.method, prior.betas, n, p, p0, y, SS0)
X.full <- X.full[obsnotNA,] #remove NA obs from null model
#check for missings and define competing variables with NAs
NAvars <- checkformissings(y = framenull[,1], framenull[,-1], X.full)
#Define function to get binary expression for each model
num2bin.model.fun <- function (x) num2bin.model(x, p = p,
namesxnotnull = namesxnotnull,
NAvars = NAvars)
#Imputation step
if (anyNAvar <- !is.null(NAvars)) {
if (is.null(imp.datasets)) { #if there are no given imputations, build them
imputation.list <- buildimputation(NAvars, formula, data, imp.predict.mat, n.imp,
maxit, n, q, p0, imp.mice.method, imp.seed,
parallelmice, n.core, obsnotNA, ordvars)
} else imputation.list <- extimputation(formula, imp.datasets, n0 = dim(data)[1],
framefull, ordvars, obsnotNA, p0, NAvars)
list2env(imputation.list, envir = env)
}
if (n.imp > 1) {
#function to compute log(BFa0) for a given model as an average of BF computed
#by lBF over the imputed datasets
lBF.method <- function (model) lBF.av(model,
imputation.array = imputation.array,
lBF = lBF, p0 = p0, n.imp = n.imp)
} else lBF.method <- function (model) lBF(k = length(model),
X = imputation.array[,c(1:p0, model+p0),])
#Info:
cat("Info. . .\n")
if (mF) {
cat("Most complex model has a total of", q + q0, "covariates and/or factors.\n")
} else cat("Most complex model has a total of", q + q0, "competing variables.\n")
if (q0 == 1) {
cat("From those 1 is fixed (the intercept) and we should select from the remaining",
q, ".\n")
} else cat("From those", q0, "are fixed and we should select from the remaining",
q, ".\n")
if (mF) {
cat("  Numerical covariates:", depvars[positionsx], "\n")
cat(" Factors:", depvars[!positionsx], "\n")
} else  cat("  Competing variables:", depvars, "\n")
cat("The problem has a total of", 2^q, "competing models.\n")
cat("Of these, the ", n.keep, "most probable (a posteriori) are kept.\n")
#Compute exact posterior distribution and normalizing constant
posterior.list <- exact.posterior.comput(p, num2bin.model.fun, lBF.method,
lp.model, X0, X.full, lBF)
list2env(posterior.list, envir = env)
#compute models matrix at the covariate-factor level
cf.models.PM <- all.models.PM[,seq_len(p)] %*% t(positions)
cf.models.PM <- cbind(cf.models.PM, all.models.PM[,p+1])
colnames(cf.models.PM)[q+1] <- "Post"
cf.models.PM
all.models.PM
data = dataS97
formula <- as.formula(formula)
null.model <- as.formula(null.model)
#Response in the null model and full model must coincide
if (formula[[2]] != null.model[[2]]){
stop("The response in the full and null model does not coincide.\n")
}
env <- environment()
#Build matrices and objects needed later on
buildmatrices.list <- buildmatrices(formula, null.model, data, marginal.factors)
list2env(buildmatrices.list, envir = env)
#Check arguments and compute n.keep if needed
n.keep <- checkBvsarguments(p, p0, namesnull, namesx, n.keep, q)
#Check model priors chosen and define the functions to be used
lprior.models <- checkforprior.models(prior.models, priorprobs, q)
mF <- L > 0 & marginal.factors
#Check if factors present and if marginalization of their probabilities. Define model prior
lp.model <- checkmarg.factorsprior(mF, prior.models.dummies, l, positions[positionsx,,drop = F],
positionsfac, satmodels.repr, lprior.models)
#Evaluate the null model:
lmnull <- lm(formula = null.model, data, y = TRUE, x = TRUE)
#The response variable
y <- lmnull$y; obsnotNA <- names(y) #without missings
n <- length(y)
SS0 <- crossprod(lmnull$residuals) #SSE of the null model
#Check approx method and priors chosen and define the function to be used
lBF <- checkforprior.betas.lm(BF.method, prior.betas, n, p, p0, y, SS0)
X.full <- X.full[obsnotNA,] #remove NA obs from null model
#check for missings and define competing variables with NAs
NAvars <- checkformissings(y = framenull[,1], framenull[,-1], X.full)
#Define function to get binary expression for each model
num2bin.model.fun <- function (x) num2bin.model(x, p = p,
namesxnotnull = namesxnotnull,
NAvars = NAvars)
#Imputation step
if (anyNAvar <- !is.null(NAvars)) {
if (is.null(imp.datasets)) { #if there are no given imputations, build them
imputation.list <- buildimputation(NAvars, formula, data, imp.predict.mat, n.imp,
maxit, n, q, p0, imp.mice.method, imp.seed,
parallelmice, n.core, obsnotNA, ordvars)
} else imputation.list <- extimputation(formula, imp.datasets, n0 = dim(data)[1],
framefull, ordvars, obsnotNA, p0, NAvars)
list2env(imputation.list, envir = env)
}
if (n.imp > 1) {
#function to compute log(BFa0) for a given model as an average of BF computed
#by lBF over the imputed datasets
lBF.method <- function (model) lBF.av(model,
imputation.array = imputation.array,
lBF = lBF, p0 = p0, n.imp = n.imp)
} else lBF.method <- function (model) lBF(k = length(model),
X = imputation.array[,c(1:p0, model+p0),])
#Info:
cat("Info. . .\n")
if (mF) {
cat("Most complex model has a total of", q + q0, "covariates and/or factors.\n")
} else cat("Most complex model has a total of", q + q0, "competing variables.\n")
if (q0 == 1) {
cat("From those 1 is fixed (the intercept) and we should select from the remaining",
q, ".\n")
} else cat("From those", q0, "are fixed and we should select from the remaining",
q, ".\n")
if (mF) {
cat("  Numerical covariates:", depvars[positionsx], "\n")
cat(" Factors:", depvars[!positionsx], "\n")
} else  cat("  Competing variables:", depvars, "\n")
cat("The problem has a total of", 2^q, "competing models.\n")
cat("Of these, the ", n.keep, "most probable (a posteriori) are kept.\n")
#Compute exact posterior distribution and normalizing constant
posterior.list <- exact.posterior.comput(p, num2bin.model.fun, lBF.method,
lp.model, X0, X.full, lBF)
list2env(posterior.list, envir = env)
#compute models matrix at the covariate-factor level
cf.models.PM <- all.models.PM[,seq_len(p)] %*% t(positions)
cf.models.PM <- cbind(cf.models.PM, all.models.PM[,p+1])
colnames(cf.models.PM)[q+1] <- "Post"
cf.models.PM
modelsprob <- buildmodelsmatrix(q)
modelsprob
positionsf <- colSums(positions %*% t(positions)) > 1 #TRUE if factor
positionsf
i <- 2
modeli <- modelsprob[i, seq_len(q)]
modeli
j <- which(apply(cf.models.PM[, seq_len(q)], 1, function(x) all(x == modeli)))
j
positionsf <- colSums(positions %*% t(positions)) > 1 #TRUE if factor
#search models on modelsprob
for (i in seq_len(2^q)) {
modeli <- modelsprob[i, seq_len(q)]
j <- which(apply(cf.models.PM[, seq_len(q)], 1, function(x) all(x == modeli)))
modelsprob[i, q + 1] <- sum(cf.models.PM[j, q + 1])
}
modelsprob
cf.models.PM[j, q + 1]
j<-j-1
cf.models.PM[j, q + 1]
j<-2
cf.models.PM[j, q + 1]
i<-2
modeli <- modelsprob[i, seq_len(q)]
j <- which(apply(cf.models.PM[, seq_len(q)], 1, function(x) all(x == modeli)))
cf.models.PM[j, q + 1]
devtools::load_all()
#Compute exact posterior distribution and normalizing constant
posterior.list <- exact.posterior.comput(p, num2bin.model.fun, lBF.method,
lp.model, X0, X.full, lBF)
list2env(posterior.list, envir = env)
#Summ up the posterior distribution
summ.posterior.list <- summ.posterior(all.models.PM, p, q, mF, positions, num2bin.model.fun)
list2env(summ.posterior.list, envir = env)
if (anyNAvar) {#Pool results for imputed datasets
imp.array <- imputation.array
#if marfinal factor probs, remove first dummy on each factor, first p0 are the fixed ones
if (mF) imp.array <- imp.array[,-c(indf + p0), , drop = FALSE]
#Evaluate lm of full model with missings using Rubin's rule
fit <- list(); mt <- attr(framefull, "terms")
for (i in 1:n.imp) {
z <- lm.fit(x = imp.array[,,i], y = y)
z$terms <- mt; class(z) <- "lm"; fit[[i]] <- z
}
lmfull <- mice::pool(fit)
lmfull$call <- NULL #otherwise, Rstudio returns a warning trying to read lmfull$call
} else lmfull <- lm(formula, data, x = TRUE, y = TRUE)
?match
modeli
match(modeli, cf.models.PM[, seq_len(q)])
?tabular
?tabulate
tabulate(c(2,3,5))
apply(cf.models.PM[,seq_len(q)] > 0, 1, digest::digest)
cf.models.PM[,seq_len(q)]
modelhashes <- apply(cf.models.PM[,seq_len(q)] > 0, 1, digest::digest)
?match
modeli <- modelsprob[i, seq_len(q)]
digest::digest(modeli)
match(digest::digest(modeli), modelhasehes)
match(digest::digest(modeli), modelhashes)
modeli
modeli <- modelsprob[i, seq_len(q)] > 0
match(digest::digest(modeli), modelhashes)
cf.models.PM[,seq_len(q)]
match(modelhashes)
modelhashes
match(2,c(2,3,2))
modeli <- digest::digest(modelsprob[i, seq_len(q)] > 0)
modeli
modeli %in% modelhashes
cf.models.PM[,seq_len(q)] > 0
modelsprob[i, seq_len(q)]
modelhashes
unique(modelhashes)
modelsprob
digest::digest(c(1,0,0))
digest::digest(c(1,0,0)>0)
?digest::digest
cf.models.PM[,seq_len(q)]
modelhashes
digest::digest(cf.models.PM[1,seq_len(q)]>0)
digest::digest(cf.models.PM[1,seq_len(q)]>0)
digest::digest(modelsprob[i, seq_len(q)] > 0)
i
digest::digest(modelsprob[1, seq_len(q)] > 0)
modelsprob
digest::digest(modelsprob[, seq_len(q)] > 0)
modelhashes <- apply(modelsprob[,seq_len(q)] > 0, 1, digest::digest)
modelhashes
modelhashes <- apply(cf.models.PM[,seq_len(q)] > 0, 1, digest::digest, algo = "xxhash64")
modelhashes
apply(modelsprob[,seq_len(q)] > 0, 1, digest::digest, algo = "xxhash64")
modelhashes <- apply(as.numeric(cf.models.PM[,seq_len(q)] > 0), 1, digest::digest, algo = "xxhash64")
modelhashes <- apply(as.numeric(cf.models.PM[,seq_len(q)] > 0), 1, digest::digest, algo = "xxhash64")
modelhashes <- apply(as.numeric(cf.models.PM[,seq_len(q)]) > 0, 1, digest::digest, algo = "xxhash64")
