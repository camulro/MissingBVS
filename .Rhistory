blocks
?as.list
blocks <- as.list(visit.seq, all.names = TRUE)
blocks
if (!is.null(visit.seq)) {
blocks <- as.list(visit.seq); names(blocks) <- visit.seq
}
if (!is.null(visit.seq)) {
blocks <- as.list(visit.seq); names(blocks) <- visit.seq
} else blocks <- NULL
imput <- mice::futuremice(X,
meth = imp.mice.method,
m = n.imp,
predictorMatrix = imp.predict.mat,
visitSequence = visit.seq,
blocks = blocks,
parallelseed = seed,
n.core = n.core)
imput$imp
blocks
mice::complete(imput, action = "all") #extracts all at once
imputnop$imp
imputnop$blocks
imput$blocks
imput$visitSequence
imput$predictorMatrix
imputnop$predictorMatrix
?futuremice
imput <- mice::futuremice(X,
meth = imp.mice.method,
m = n.imp,
predictorMatrix = imp.predict.mat,
visitSequence = "roman",
blocks = blocks,
parallelseed = seed,
n.core = n.core)
mice::complete(imput, action = "all") #extracts all at once
imput <- mice::futuremice(X,
meth = imp.mice.method,
m = n.imp,
predictorMatrix = imp.predict.mat,
# visitSequence = visit.seq,
blocks = blocks,
parallelseed = seed,
n.core = n.core)
mice::complete(imput, action = "all") #extracts all at once
imp.predict.mat
?list
blocks <- list()
for (j in colnames(imp.predict.mat)) {
blocks[[j]] <- NULL
}
blocks
colnames(imp.predict.mat)
blocks[[j]]
blocks <- list()
for (j in 1:dim(imp.predict.mat)[2]) {
if (imp.pred.col[j] %in% visit.seq) {
blocks[[j]] <- imp.pred.col[j]
} else blocks[[j]] <- NULL
names(blocks)
}
blocks
dim(imp.predict.mat)[2]
blocks <- list(); names(blocks) <- imp.pred.col
blocks <- list(NULL); names(blocks) <- imp.pred.col
imputnop$blocks
imputnop$imp
imputnop$where
imput$where
imput$method
impunopt$method
imputnop$method
imputnop$ignore
imputnop$post
imput$post
imput$predictorMatrix
imput$blots
imputnop$blots
imputnop$blocks
imputç$blocks
imput$blocks
imput$visitSequence
imputnop$visitSequence
imputnop$formulas
imput$formulas
imput$nmis
imput$where
imp.predict.mat
mice::mice(X,
meth = imp.mice.method,
m = n.imp,
predictorMatrix = imp.predict.mat,
# visitSequence = visit.seq,
# blocks = blocks,
printFlag = FALSE,
seed = seed)
imput <- mice::mice(X,
meth = imp.mice.method,
m = n.imp,
predictorMatrix = imp.predict.mat,
# visitSequence = visit.seq,
blocks = blocks,
printFlag = FALSE,
seed = seed)
if (!is.null(visit.seq)) { #build blocks list according to visitSequence
blocks <- as.list(visit.seq); names(blocks) <- visit.seq
} else blocks <- NULL
imput <- mice::mice(X,
meth = imp.mice.method,
m = n.imp,
predictorMatrix = imp.predict.mat,
# visitSequence = visit.seq,
blocks = blocks,
printFlag = FALSE,
seed = seed)
imput$imp
imput <- mice::futuremice(X,
meth = imp.mice.method,
m = n.imp,
predictorMatrix = imp.predict.mat,
# visitSequence = visit.seq,
blocks = blocks,
parallelseed = seed,
n.core = n.core)
imput$imp
mice::complete(imput, action = "all")
imput$call
imput$visitSequence
mice(X, maxit = 0)
dryrun <- mice(X, maxit = 0)
dryrun$blocks
dryrun$blocks[[imp.pred.col]]
imp.pred.col
imput <- mice::futuremice(X,
meth = imp.mice.method,
m = n.imp,
predictorMatrix = imp.predict.mat,
blocks = dryrun$blocks,
parallelseed = seed,
n.core = n.core)
blocks
mice::mice(X,
meth = imp.mice.method,
m = n.imp,
predictorMatrix = imp.predict.mat,
blocks = blocks,
printFlag = FALSE,
seed = seed)
imput <- mice::mice(X,
meth = imp.mice.method,
m = n.imp,
predictorMatrix = imp.predict.mat,
blocks = blocks,
printFlag = FALSE,
seed = seed)
imput$imp
dryrun$imp
dryrun <- mice(X, maxit = 0)
dryrun <- mice(X, maxit = 0)
dryrun$imp
imput$imp
imput$imp
names(imput$imp)
dryrun$imp
dryrun$imp <- ifelse(names(dryrun$imp) %notin% visit.seq, NULL, dryrun$imp )
imput <- mice::futuremice(X,
meth = imp.mice.method,
m = n.imp,
predictorMatrix = imp.predict.mat,
# blocks = blocks,
parallelseed = seed,
n.core = n.core)
imp.predict.mat = matrix(NA, 3, 3, dimnames = list(c("lifee060","EcOrg","laam"),c("lifee060","laam","EcOrg")))
imp.predict.mat[1,] <- c(0,1,1)
imp.predict.mat[2,] <- c(1,1,0)
imp.predict.mat[3,] <- c(1,0,1)
#Impute just competing variables with NAs
fulldataframe <- model.frame(paste0(formula[[2]], "~."), data, na.action = NULL)
X.toimp <- fulldataframe[,-1] #full observed design matrix
#Default prediction matrix by mice:
quickpredict.mat <- mice::quickpred(X.toimp); imp.pred.col <- NULL
#check predict imputation matrix
imp.vars <- rownames(imp.predict.mat)
if (any(NAvars %notin% imp.vars)) {
stop(paste0("Imputation prediction matrix rows given do not contain all the variables ",
"given by formula with NAs.", "Make sure to include them all.\n"))
}
imp.pred.col <- colnames(imp.predict.mat)
quickpredict.mat[imp.vars, imp.pred.col] <- imp.predict.mat
quickpredict.mat[imp.vars, colnames(quickpredict.mat) %notin% imp.pred.col] <- 0
quickpredict.mat
not.imp.vars <- setdiff(attr(terms(fulldataframe), "term.labels"), imp.vars)
quickpredict.mat[not.imp.vars, ] <- 0
quickpredict.mat
imp.predict.mat = quickpredict.mat
imput <- mice::futuremice(X,
meth = imp.mice.method,
m = n.imp,
predictorMatrix = imp.predict.mat,
# blocks = blocks,
parallelseed = seed,
n.core = n.core)
imps <- mice::complete(imput, action = "all") #extracts all at once
imps
blocks
imput <- mice::futuremice(X,
meth = imp.mice.method,
m = n.imp,
predictorMatrix = imp.predict.mat,
blocks = blocks,
parallelseed = seed,
n.core = n.core)
visit.seq
imput <- mice::futuremice(X,
meth = imp.mice.method,
m = n.imp,
predictorMatrix = imp.predict.mat,
visitSequence = visit.seq,
# blocks = blocks,
parallelseed = seed,
n.core = n.core)
imps <- mice::complete(imput, action = "all") #extracts all at once
not.imp.vars
imp.vars
imp.pred.col
#visit sequence given by order of columns in imp.predict.mat, if given
visit.seq <- c(imp.pred.col, not.imp.vars)
imput <- mice::futuremice(X,
meth = imp.mice.method,
m = n.imp,
predictorMatrix = imp.predict.mat,
visitSequence = visit.seq,
# blocks = blocks,
parallelseed = seed,
n.core = n.core)
imps <- mice::complete(imput, action = "all") #extracts all at once
imput$visitSequence
imput$blocks
imput$predictorMatrix
imps
imput$method
?mice
imp.mice.method
if (length(imp.mice.method) == 1) imp.mice.method <- rep(imp.mice.method, ncol(quickpredict.mat))
imp.mice.method
quickpredict.mat
visit.seq
quickpredict.mat
imp.predict.mat = quickpredict.mat
imput <- mice::mice(X,
meth = imp.mice.method,
m = n.imp,
predictorMatrix = imp.predict.mat,
visitSequence = visit.seq,
printFlag = FALSE,
seed = seed)
imput$visitSequence
imput$method
imput$predictorMatrix
if (length(imp.mice.method) == 1) imp.mice.method <- rep(imp.mice.method, ncol(imp.predict.mat))
imp.mice.method
rowSums(imp.predict.mat)
imp.mice.method = "pmm"
if (length(imp.mice.method) == 1) ifelse(rowSums(imp.predict.mat)>0, imp.mice.method, "")
imput <- mice::mice(X,
meth = imp.mice.method,
m = n.imp,
predictorMatrix = imp.predict.mat,
visitSequence = visit.seq,
printFlag = FALSE,
seed = seed)
imput$imp
imput2 <- mice::mice(X,
meth = "pmm",
m = n.imp,
predictorMatrix = imp.predict.mat,
visitSequence = visit.seq,
printFlag = FALSE,
seed = seed)
imput2$imp$gvxdxe52
X[,ncol(X)]
imput$blocks
imput <- mice::mice(X,
meth = imp.mice.method,
m = n.imp,
predictorMatrix = imp.predict.mat,
visitSequence = NULL,
printFlag = FALSE,
seed = seed)
imput$visitSequence
visit.seq %in% imput$visitSequence
imput$loggedEvents
imput$chainVar
imput$chainMean
imput$method
imput$iteration
?mice
imput$loggedEvents
load_all()
#una vez documentadas las funciones con el formato adecuado,
#convertir esos xorygen comments creando un nuevo archivo.Rd:
#No es necesario documentar todas las funciones, solo aquellas funciones que
#queramos que estén disponibles y visibles para los usuarios
devtools::document()
devtools::load_all() # para cargar las funciones de la librería
f <- gr56092 ~ 1 + lifee060 + gdpsh60l + p60
dataS97.mBVS <- missingBVS.lm(formula = f, data = dataS97, n.keep = 8)
imp.predict.mat
rm(list = ls())
load_all()
f <- gr56092 ~ 1 + lifee060 + gdpsh60l + p60
dataS97.mBVS <- missingBVS.lm(formula = f, data = dataS97, n.keep = 8)
imp.predict.mat = matrix(NA, 3, 3, dimnames = list(c("lifee060","EcOrg","laam"),c("lifee060","laam","EcOrg")))
imp.predict.mat[1,] <- c(0,1,1)
imp.predict.mat[2,] <- c(1,1,0)
imp.predict.mat[3,] <- c(1,0,1)
formula <- gr56092 ~ 1 + lifee060 + EcOrg + laam
data <- dataS97
data$EcOrg <- factor(data$EcOrg)
null.model = paste(as.formula(formula)[[2]], " ~ 1", sep="")
BF.approx.method = "gprior"
prior.betas = "Robust"
prior.models = "ScottBerger"
prior.models.dummies = "ScottBerger"
marginal.factors = TRUE
priorprobs = NULL
n.keep = "all"
parallelmice = NULL
n.core = NULL
imp.time.test = TRUE
imp.mice.method = "pmm"
imp.predict.mat = NULL
n.imp = 9
imp.datasets = NULL
imp.seed = 1
imp.predict.mat = matrix(NA, 3, 3, dimnames = list(c("lifee060","EcOrg","laam"),c("lifee060","laam","EcOrg")))
imp.predict.mat[1,] <- c(0,1,1)
imp.predict.mat[2,] <- c(1,1,0)
imp.predict.mat[3,] <- c(1,0,1)
load_all()
formula <- as.formula(formula)
null.model <- as.formula(null.model)
#Response in the null model and full model must coincide
if (formula[[2]] != null.model[[2]]){
stop("The response in the full and null model does not coincide.\n")
}
env <- environment()
#Build matrices and objects needed later on
buildmatrices.list <- buildmatrices(formula, null.model, data, marginal.factors)
list2env(buildmatrices.list, envir = env)
#Check arguments and compute n.keep if needed
n.keep <- checkBvsarguments(p, p0, namesnull, namesx, n.keep, q)
#Check model priors chosen and define the functions to be used
lprior.models <- checkforprior.models(prior.models, priorprobs, q)
mF <- L > 0 & marginal.factors
#Check if factors present and if marginalization of their probabilities. Define model prior
lp.model <- checkmarg.factorsprior(mF, prior.models.dummies, l, positions, positionsfac,
firstd, lprior.models)
#Evaluate the null model:
lmnull <- lm(formula = null.model, data, y = TRUE, x = TRUE)
#The response variable
y <- lmnull$y; obsnotNA <- names(y) #without missings
n <- length(y)
SS0 <- crossprod(lmnull$residuals) #SSE of the null model
#Check approx method and priors chosen and define the function to be used
BF.approx.method <- checkforprior.betas.lm(BF.approx.method, prior.betas, n, p, p0, y, SS0)
X.full <- X.full[obsnotNA,] #remove NA obs from null model
#check for missings and define competing variables with NAs
NAvars <- checkformissings(y = framenull[,1], framenull[,-1], X.full)
#Imputation step
if (anyNAvar <- !is.null(NAvars)) {
if (is.null(imp.datasets)) { #if there are no given imputations, build them
imputation.list <- buildimputation(NAvars, formula, data, imp.predict.mat, n.imp, maxit,
n, q, p0, imp.time.test, imp.mice.method, imp.seed,
parallelmice, n.core, obsnotNA, ordvars, BF.approx.method)
} else imputation.list <- extimputation(formula, imp.datasets, n0 = dim(data)[1], framefull,
ordvars, obsnotNA, p0, BF.approx.method, NAvars)
list2env(imputation.list, envir = env)
}
maxit = 5
#Imputation step
if (anyNAvar <- !is.null(NAvars)) {
if (is.null(imp.datasets)) { #if there are no given imputations, build them
imputation.list <- buildimputation(NAvars, formula, data, imp.predict.mat, n.imp, maxit,
n, q, p0, imp.time.test, imp.mice.method, imp.seed,
parallelmice, n.core, obsnotNA, ordvars, BF.approx.method)
} else imputation.list <- extimputation(formula, imp.datasets, n0 = dim(data)[1], framefull,
ordvars, obsnotNA, p0, BF.approx.method, NAvars)
list2env(imputation.list, envir = env)
}
parallelmice = TRUE
#Imputation step
if (anyNAvar <- !is.null(NAvars)) {
if (is.null(imp.datasets)) { #if there are no given imputations, build them
imputation.list <- buildimputation(NAvars, formula, data, imp.predict.mat, n.imp, maxit,
n, q, p0, imp.time.test, imp.mice.method, imp.seed,
parallelmice, n.core, obsnotNA, ordvars, BF.approx.method)
} else imputation.list <- extimputation(formula, imp.datasets, n0 = dim(data)[1], framefull,
ordvars, obsnotNA, p0, BF.approx.method, NAvars)
list2env(imputation.list, envir = env)
}
imp.predict.mat <- NULL
#Imputation step
if (anyNAvar <- !is.null(NAvars)) {
if (is.null(imp.datasets)) { #if there are no given imputations, build them
imputation.list <- buildimputation(NAvars, formula, data, imp.predict.mat, n.imp, maxit,
n, q, p0, imp.time.test, imp.mice.method, imp.seed,
parallelmice, n.core, obsnotNA, ordvars, BF.approx.method)
} else imputation.list <- extimputation(formula, imp.datasets, n0 = dim(data)[1], framefull,
ordvars, obsnotNA, p0, BF.approx.method, NAvars)
list2env(imputation.list, envir = env)
}
parallelmice = F
#Imputation step
if (anyNAvar <- !is.null(NAvars)) {
if (is.null(imp.datasets)) { #if there are no given imputations, build them
imputation.list <- buildimputation(NAvars, formula, data, imp.predict.mat, n.imp, maxit,
n, q, p0, imp.time.test, imp.mice.method, imp.seed,
parallelmice, n.core, obsnotNA, ordvars, BF.approx.method)
} else imputation.list <- extimputation(formula, imp.datasets, n0 = dim(data)[1], framefull,
ordvars, obsnotNA, p0, BF.approx.method, NAvars)
list2env(imputation.list, envir = env)
}
imp.predict.mat
#Impute just competing variables with NAs
fulldataframe <- model.frame(paste0(formula[[2]], "~."), data, na.action = NULL)
X.toimp <- fulldataframe[,-1] #full observed design matrix
#Default prediction matrix by mice:
quickpredict.mat <- mice::quickpred(X.toimp); imp.pred.col <- NULL
if (!is.null(imp.predict.mat)) { #if given by user
#check predict imputation matrix
imp.vars <- rownames(imp.predict.mat)
if (any(NAvars %notin% imp.vars)) {
stop(paste0("Imputation prediction matrix rows given do not contain all the variables ",
"given by formula with NAs.", "Make sure to include them all.\n"))
}
imp.pred.col <- colnames(imp.predict.mat)
quickpredict.mat[imp.vars, imp.pred.col] <- imp.predict.mat
quickpredict.mat[imp.vars, colnames(quickpredict.mat) %notin% imp.pred.col] <- 0
# quickpredict.mat <- quickpredict.mat[imp.vars,]
} else imp.vars <- NAvars
imp.vars
quickpredict.mat
not.imp.vars
#Do not impute variables with missings that are not in imp.vars (also in NAvars)
not.imp.vars <- setdiff(attr(terms(fulldataframe), "term.labels"), imp.vars)
not.imp.vars
quickpredict.mat[not.imp.vars, ] <- 0
quickpredict.mat
formula
quickpredict.mat["EcOrg",]
Avars
NAvars
imp.pred.col
#visit sequence given by order of columns in imp.predict.mat, if given
visit.seq <- c(imp.pred.col, not.imp.vars)
visit.seq
imp.pred.col
c(imp.pred.col, not.imp.vars)
NAvars
NAvars %in% visit.seq
quickpredict.mat
#visit sequence given by order of columns in imp.predict.mat, if given
visit.seq <- c(imp.vars, not.imp.vars)
loggedEvents <- NULL #to save the report of possible problems identified by mice
imputation.array <-  mice.imputation(X = X.toimp, formula,
n.imp = n.imp,
imp.predict.mat = quickpredict.mat,
imp.mice.method = imp.mice.method,
visit.seq = visit.seq,
seed = imp.seed,
maxit = maxit,
parallel = parallelmice,
n.core = n.core)
loggedEvents
parallelmice
parallelmice <- TRUE
imputation.array <-  mice.imputation(X = X.toimp, formula,
n.imp = n.imp,
imp.predict.mat = quickpredict.mat,
imp.mice.method = imp.mice.method,
visit.seq = visit.seq,
seed = imp.seed,
maxit = maxit,
parallel = parallelmice,
n.core = n.core)
loggedEvents
load_all()
f <- gr56092 ~ 1 + lifee060 + gdpsh60l + p60
dataS97.mBVS <- missingBVS.lm(formula = f, data = dataS97, n.keep = 8)
dataS97.mBVS$imp.args$loggedEvents
load_all()
f <- gr56092 ~ 1 + lifee060 + gdpsh60l + p60
dataS97.mBVS <- missingBVS.lm(formula = f, data = dataS97, n.keep = 8)
dataS97.mBVS$imp.args$loggedEvents
imput$loggedEvents
load_all()
dataS97.mBVS <- missingBVS.lm(formula = f, data = dataS97, n.keep = 8, n.imp = 5)
dataS97.mBVS$imp.args$loggedEvents
load_all()
dataS97.mBVS <- missingBVS.lm(formula = f, data = dataS97, n.keep = 8, n.imp = 5)
load_all()
dataS97.mBVS <- missingBVS.lm(formula = f, data = dataS97, n.keep = 8, n.imp = 5)
load_all()
dataS97.mBVS <- missingBVS.lm(formula = f, data = dataS97, n.keep = 8, n.imp = 5)
dataS97.mBVS$imp.args$loggedEvents
check()
pkgbuild::check_build_tools(debug = TRUE)
check()
check()
usethis::use_testthat(3)
